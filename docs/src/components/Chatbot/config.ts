/**
 * Configuration for the Chatbot component
 */
export const BACKEND_CONFIG = {
  baseUrl: process.env.REACT_APP_BACKEND_URL || process.env.BACKEND_URL || process.env.NEXT_PUBLIC_BACKEND_URL || 'https://nainee-chatbot.hf.space',
};